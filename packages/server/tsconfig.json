{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "types": ["./worker-configuration.d.ts", "bun"],

    // api-specパッケージのopenapi-typescriptで生成された型定義ファイルをインポートできるようにするための設定
    "paths": {
      "@api-spec/*": ["../api-spec/src/*.ts"]
    }
  }
}
